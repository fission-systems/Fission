# Changelog scratch area (archived dated entries)

Historical date-stamped development logs that lived here were moved to
[`../Legacy/`](../Legacy/) (same filenames: `YYYYMMDD_Changelog.md`).

Going forward, prefer **version-scoped** release notes (for example SemVer tags,
GitHub Releases, or a root `CHANGELOG.md` once adopted) instead of adding new
daily files under this directory.
