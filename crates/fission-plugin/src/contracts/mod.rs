//! Stable plugin traits and metadata types (`PluginAPI`, `FissionPlugin`, `PluginInfo`).
//!
//! Loading and hooks live in the `plugin` module when `interactive_runtime` is enabled.

pub mod traits;
pub mod types;
