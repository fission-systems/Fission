name: Typos Check

on: [push, pull_request]

jobs:
    typos:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: Check for typos
              uses: crate-ci/typos@master
              # 기본 설정을 사용하며, 필요시 typos.toml 로 예외 처리 가능
