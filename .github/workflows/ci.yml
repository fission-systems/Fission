name: CI Fast Gate

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

concurrency:
  group: ci-fast-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  linux-fast-gate:
    name: Linux Fast Gate
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Cache Rust build outputs
        uses: Swatinem/rust-cache@v2

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: crates/fission-tauri/package-lock.json

      - name: Install cargo-deny
        run: cargo install cargo-deny --locked

      - name: Security advisory check (Rust — advisories, bans, licenses, sources)
        run: cargo deny check all

      - name: Security advisory check (Node)
        run: |
          cd crates/fission-tauri
          npm ci --ignore-scripts
          npm audit --audit-level=high

      - name: Install system dependencies (Linux/Tauri)
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libssl-dev \
            librsvg2-dev \
            libayatana-appindicator3-dev \
            build-essential \
            file

      - name: Build Decompiler
        run: |
          cmake -S ghidra_decompiler -B ghidra_decompiler/build -DCMAKE_BUILD_TYPE=Release
          cmake --build ghidra_decompiler/build --config Release

      - name: Check Formatting
        run: cargo fmt -- --check

      - name: Lint with Clippy
        run: cargo clippy --workspace --exclude fission-tauri -- -D warnings

      - name: Run Tests
        run: cargo test -p fission-pcode -p fission-automation -p fission-loader --verbose

      - name: Fission decompilation smoke test
        run: |
          chmod +x scripts/test/decomp_smoke_ci.sh
          ./scripts/test/decomp_smoke_ci.sh

  windows-build-test:
    name: Windows Build/Test
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust build outputs
        uses: Swatinem/rust-cache@v2

      - name: Build Decompiler (Windows)
        shell: pwsh
        run: |
          vcpkg install zlib:x64-windows
          cd ghidra_decompiler
          if (!(Test-Path build)) { New-Item -ItemType Directory -Path build | Out-Null }
          cd build
          cmake .. -DCMAKE_TOOLCHAIN_FILE="$env:VCPKG_INSTALLATION_ROOT/scripts/buildsystems/vcpkg.cmake"
          cmake --build . --config Release --target decomp fission_decomp

      - name: Build fission-cli (release)
        run: cargo build -p fission-cli --features native_decomp --release

      - name: Run Windows core tests
        run: cargo test -p fission-pcode -p fission-automation --release --verbose

  macos-build-test:
    name: Build and Test (macOS)
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Cache Rust build outputs
        uses: Swatinem/rust-cache@v2

      - name: Build Decompiler
        run: |
          cmake -S ghidra_decompiler -B ghidra_decompiler/build -DCMAKE_BUILD_TYPE=Release
          cmake --build ghidra_decompiler/build --config Release

      - name: Check Formatting
        run: cargo fmt -- --check

      - name: Lint with Clippy
        run: cargo clippy --workspace --exclude fission-tauri -- -D warnings

      - name: Run Tests
        run: cargo test -p fission-pcode -p fission-automation -p fission-loader --verbose
