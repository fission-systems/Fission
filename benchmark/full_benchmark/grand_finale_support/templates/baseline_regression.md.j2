# Benchmark Regression Gate vs Baseline

- Status: {{ payload.get("status", "unknown") }}
- Similarity regression threshold: {{ "%.3f"|format((payload.get("threshold_pp", 0.0) or 0.0) | float) }}pp

## Regression Reasons

{% for item in payload.get("regressions", []) %}
- {{ item }}
{% else %}
- No baseline regression detected.
{% endfor %}

## Row Fidelity Gate

{% set row_gate = payload.get("row_fidelity_gate", {}) or {} %}
- Status: {{ row_gate.get("status", "unknown") }}
- Failed targets: {{ row_gate.get("failed_target_count", 0) }}

| Role | Address | Prev sim | Cur sim | Delta | Status | Reasons |
| --- | --- | ---: | ---: | ---: | --- | --- |
{% for row in row_gate.get("rows", []) %}
| {{ row.get("role", "") }} | {{ row.get("address", "") }} | {{ "%.3f"|format((row.get("previous_normalized_similarity", 0.0) or 0.0) | float) }}% | {{ "%.3f"|format((row.get("current_normalized_similarity", 0.0) or 0.0) | float) }}% | {{ "%+.3f"|format((row.get("normalized_similarity_delta", 0.0) or 0.0) | float) }}pp | {{ row.get("status", "") }} | {{ ", ".join(row.get("failure_reasons", [])) or "none" }} |
{% endfor %}
