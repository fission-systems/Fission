# Whole Decomp Benchmark: {{ summary.binary or "unknown" }}

## Summary

- Generated: {{ summary.generated_at or "unknown" }}
- Public summary: {{ summary.public_summary_line or "n/a" }}
- Avg normalized similarity: {{ "%.3f"|format(quality.get("avg_normalized_similarity", 0.0) | float) }}%
- Aggregate normalized similarity: {{ "%.3f"|format(quality.get("aggregate_normalized_similarity", 0.0) | float) }}%
- Coverage: {{ "%.3f"|format(coverage.get("coverage_ratio_pct", 0.0) | float) }}%

{% if baseline_gate %}
## Baseline Gate

- Status: `{{ baseline_gate.get("status", "unknown") }}`
- Promotion blockers:
{% for item in baseline_gate.get("regressions", []) %}
- {{ item }}
{% else %}
- none
{% endfor %}
{% endif %}

## Owner Metrics

| Metric | Fission |
| --- | ---: |
{% for key, value in owner_metrics|dictsort %}
| `{{ key }}` | {{ "%.3f"|format(value|float) }} |
{% endfor %}

## Shape-Drift Proxies

Presentation-oriented proxies only; these are not semantic truth.

| Metric | Fission |
| --- | ---: |
{% for key, value in shape_drift_metrics|dictsort %}
| `{{ key }}` | {{ "%.3f"|format(value|float) }} |
{% endfor %}

{% set normalize_pass_metrics = (summary.normalize_pass_metrics or {}).get("fission", {}) or {} %}
## Normalize Pass Metrics

| Metric | Fission |
| --- | ---: |
{% for key, value in normalize_pass_metrics|dictsort %}
| `{{ key }}` | {{ "%.3f"|format(value|float) }} |
{% endfor %}

## Ghidra Concept Stage Metrics

Clean-room owner mapping only; Fission remains Rust-native and does not depend on Ghidra runtime code.

| Metric | Fission |
| --- | ---: |
{% for key, value in ghidra_action_metrics|dictsort %}
| `{{ key }}` | {{ "%.3f"|format(value|float) }} |
{% endfor %}

## MIR Shadow Metrics

Behavior-preserving projection from current HIR into the internal MIR contract. These counters are diagnostic-only in this wave.

| Metric | Fission |
| --- | ---: |
{% for key, value in mir_metrics|dictsort %}
| `{{ key }}` | {{ "%.3f"|format(value|float) }} |
{% endfor %}

## BlockGraph Region Proof Metrics

Ghidra `BlockGraph` / `FlowBlock` clean-room proof substrate counters. These are owner diagnostics, not a separate acceptance policy.

| Metric | Fission |
| --- | ---: |
{% for key, value in blockgraph_region_metrics|dictsort %}
| `{{ key }}` | {{ "%.3f"|format(value|float) }} |
{% endfor %}

{% set alias_interleave_metrics = (summary.alias_interleave_metrics or {}).get("fission", {}) or {} %}
## Alias Interleave Metrics

Guarded-tail canonicalization blocker breakdown. These counters narrow `alias_interleave_conflict` into concrete proof-failure families.

| Metric | Fission |
| --- | ---: |
{% for key, value in alias_interleave_metrics|dictsort %}
| `{{ key }}` | {{ "%.3f"|format(value|float) }} |
{% endfor %}

{% set target_structuring_rows = summary.get("target_structuring_rows", []) or [] %}
{% if target_structuring_rows %}
### Target Structuring Rows

| Address | Name | Current sim | Previous sim | Delta | Status | Code changed | Structuring ms | Forced linear | Rendered len |
| --- | --- | ---: | ---: | ---: | --- | --- | ---: | ---: | ---: |
{% for row in target_structuring_rows %}
| `{{ row.get("address", "") }}` | `{{ row.get("name", "") }}` | {{ "%.2f"|format(row.get("current_normalized_similarity", 0.0) | float) }} | {{ "%.2f"|format(row.get("previous_normalized_similarity", 0.0) | float) }} | {{ "%.2f"|format(row.get("normalized_similarity_delta", 0.0) | float) }} | `{{ row.get("row_gate_status", "") }}` | `{{ row.get("code_changed", false) }}` | {{ "%.3f"|format(row.get("structuring_duration_ms", 0.0) | float) }} | {{ row.get("forced_linear_structuring_count", 0) }} | {{ row.get("rendered_code_len", 0) }} |
{% endfor %}
{% endif %}

{% set unchanged_target_rows = summary.get("unchanged_target_rows", []) or [] %}
{% if unchanged_target_rows %}
### Unchanged Target Rows

| Address | Name | Current sim | Previous sim | Delta |
| --- | --- | ---: | ---: | ---: |
{% for row in unchanged_target_rows %}
| `{{ row.get("address", "") }}` | `{{ row.get("name", "") }}` | {{ "%.2f"|format(row.get("current_normalized_similarity", 0.0) | float) }} | {{ "%.2f"|format(row.get("previous_normalized_similarity", 0.0) | float) }} | {{ "%.2f"|format(row.get("normalized_similarity_delta", 0.0) | float) }} |
{% endfor %}
{% endif %}

## Giant Function Diagnostics

- Candidates: {{ summary.get("giant_function_candidates", 0) }}
- Max rendered code len: {{ summary.get("max_rendered_code_len", 0) }}
- Max structuring SCC component count: {{ summary.get("max_structuring_scc_component_count", 0) }}
- Max replacement plan candidate count: {{ summary.get("max_replacement_plan_candidate_count", 0) }}
- Max materialization stabilized count: {{ summary.get("max_materialization_stabilized_count", 0) }}

{% set giant_families = summary.get("giant_function_speed_family_counts", {}) or {} %}
{% if giant_families %}
### Giant Function Families

| Family | Count |
| --- | ---: |
{% for key, value in giant_families|dictsort %}
| `{{ key }}` | {{ value }} |
{% endfor %}
{% endif %}

{% set pathological_examples = summary.get("max_pathological_examples", []) or [] %}
{% if pathological_examples %}
### Pathological Examples

| Address | Name | Family | Build ms | Normalize ms | Structuring ms | Render ms | Rendered len |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: |
{% for row in pathological_examples %}
| `{{ row.get("address", "") }}` | `{{ row.get("name", "") }}` | `{{ row.get("giant_function_speed_family", "") }}` | {{ "%.3f"|format(row.get("build_duration_ms", 0.0) | float) }} | {{ "%.3f"|format(row.get("normalize_duration_ms", 0.0) | float) }} | {{ "%.3f"|format(row.get("structuring_duration_ms", 0.0) | float) }} | {{ "%.3f"|format(row.get("render_duration_ms", 0.0) | float) }} | {{ row.get("rendered_code_len", 0) }} |
{% endfor %}
{% endif %}

## Watchlist

- Source: `{{ watchlist.get("watchlist_source", "unknown") }}`
- Bootstrap rows: {{ (watchlist.get("bootstrap_row_targets", []) or []) | length }}
- Dynamic rows: {{ (watchlist.get("dynamic_watchlist_rows", []) or []) | length }}

{% set diag = watchlist.get("watchlist_diagnostics", {}) or {} %}
{% if diag.get("selected_because_counts") %}
### Watchlist Selection Reasons

| Reason | Count |
| --- | ---: |
{% for key, value in diag.get("selected_because_counts", {})|dictsort %}
| `{{ key }}` | {{ value }} |
{% endfor %}
{% endif %}

## Artifacts

- `benchmark_summary.json`
- `benchmark_summary.md`
- `benchmark_compact_summary.json`
