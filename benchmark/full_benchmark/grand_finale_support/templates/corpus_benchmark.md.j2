# Corpus Benchmark: {{ payload.get("manifest", {}).get("name", "corpus") }}

## Corpus Summary

- Generated: {{ payload.get("generated_at", "unknown") }}
- Suite tier: `{{ payload.get("suite_tier", "release") }}`
- Gate mode: `{{ payload.get("gate_mode", "advisory") }}`
- Comparable to baseline: `{{ payload.get("comparable_to_baseline", false) }}`
- Baseline artifact: `{{ payload.get("baseline_artifact") or "none" }}`
- Release promotion allowed: `{{ payload.get("release_promotion_allowed", false) }}`
- Weighted avg normalized similarity: {{ "%.3f"|format(corpus.get("weighted_avg_normalized_similarity", 0.0) | float) }}%

## x86 / x64 Split

| Arch | Binaries | Release candidates | Weighted avg norm sim | Failed binaries |
| --- | ---: | ---: | ---: | --- |
{% for arch in ["x86", "x64"] %}
{% set row = (payload.get("arch_summary", {}) or {}).get(arch, {}) or {} %}
| `{{ arch }}` | {{ row.get("binary_count", 0) }} | {{ row.get("release_candidate_count", 0) }} | {{ "%.3f"|format(row.get("weighted_avg_normalized_similarity", 0.0) | float) }}% | {{ ", ".join(row.get("failed_binary_ids", [])) or "none" }} |
{% endfor %}

## Promotion Blockers

{% for item in payload.get("promotion_blockers", []) %}
- {{ item }}
{% else %}
- none
{% endfor %}

## Owner Metric Totals

| Metric | Total |
| --- | ---: |
{% for key, value in (payload.get("owner_metric_totals", {}) or {})|dictsort %}
| `{{ key }}` | {{ "%.3f"|format(value|float) }} |
{% endfor %}

## Shape-Drift Totals

Presentation-oriented proxies only; these are not semantic truth.

| Metric | Total |
| --- | ---: |
{% for key, value in (payload.get("shape_drift_totals", {}) or {})|dictsort %}
| `{{ key }}` | {{ "%.3f"|format(value|float) }} |
{% endfor %}

## Normalize Pass Totals

| Metric | Total |
| --- | ---: |
{% for key, value in (payload.get("normalize_pass_metric_totals", {}) or {})|dictsort %}
| `{{ key }}` | {{ "%.3f"|format(value|float) }} |
{% endfor %}

## Ghidra Concept Stage Totals

Clean-room owner mapping only; Fission remains Rust-native and does not depend on Ghidra runtime code.

| Metric | Total |
| --- | ---: |
{% for key, value in (payload.get("ghidra_action_metric_totals", {}) or {})|dictsort %}
| `{{ key }}` | {{ "%.3f"|format(value|float) }} |
{% endfor %}

## BlockGraph Region Proof Totals

Ghidra `BlockGraph` / `FlowBlock` clean-room proof substrate counters. These are owner diagnostics, not a separate acceptance policy.

| Metric | Total |
| --- | ---: |
{% for key, value in (payload.get("blockgraph_region_metric_totals", {}) or {})|dictsort %}
| `{{ key }}` | {{ "%.3f"|format(value|float) }} |
{% endfor %}

## Alias Interleave Totals

Guarded-tail canonicalization blocker breakdown. These counters narrow `canonical_alias_interleave_conflict_count` into concrete proof-failure families.

| Metric | Total |
| --- | ---: |
{% for key, value in (payload.get("alias_interleave_metric_totals", {}) or {})|dictsort %}
| `{{ key }}` | {{ "%.3f"|format(value|float) }} |
{% endfor %}

{% set target_structuring_rows = payload.get("target_structuring_rows", []) or [] %}
{% if target_structuring_rows %}
### Target Structuring Rows

| Binary | Address | Name | Current sim | Previous sim | Delta | Status | Code changed | Structuring ms | Forced linear | Rendered len |
| --- | --- | --- | ---: | ---: | ---: | --- | --- | ---: | ---: | ---: |
{% for row in target_structuring_rows %}
| `{{ row.get("binary_id", "") }}` | `{{ row.get("address", "") }}` | `{{ row.get("name", "") }}` | {{ "%.2f"|format(row.get("current_normalized_similarity", 0.0) | float) }} | {{ "%.2f"|format(row.get("previous_normalized_similarity", 0.0) | float) }} | {{ "%.2f"|format(row.get("normalized_similarity_delta", 0.0) | float) }} | `{{ row.get("row_gate_status", "") }}` | `{{ row.get("code_changed", false) }}` | {{ "%.3f"|format(row.get("structuring_duration_ms", 0.0) | float) }} | {{ row.get("forced_linear_structuring_count", 0) }} | {{ row.get("rendered_code_len", 0) }} |
{% endfor %}
{% endif %}

{% set unchanged_target_rows = payload.get("unchanged_target_rows", []) or [] %}
{% if unchanged_target_rows %}
### Unchanged Target Rows

| Binary | Address | Name | Current sim | Previous sim | Delta |
| --- | --- | --- | ---: | ---: | ---: |
{% for row in unchanged_target_rows %}
| `{{ row.get("binary_id", "") }}` | `{{ row.get("address", "") }}` | `{{ row.get("name", "") }}` | {{ "%.2f"|format(row.get("current_normalized_similarity", 0.0) | float) }} | {{ "%.2f"|format(row.get("previous_normalized_similarity", 0.0) | float) }} | {{ "%.2f"|format(row.get("normalized_similarity_delta", 0.0) | float) }} |
{% endfor %}
{% endif %}

## Giant Function Families

| Family | Count |
| --- | ---: |
{% for key, value in (payload.get("giant_function_speed_family_totals", {}) or {})|dictsort %}
| `{{ key }}` | {{ value }} |
{% else %}
| `none` | 0 |
{% endfor %}

{% set pathological_examples = payload.get("max_pathological_examples", []) or [] %}
{% if pathological_examples %}
## Pathological Examples

| Binary | Address | Name | Family | Build ms | Normalize ms | Structuring ms | Render ms | Rendered len |
| --- | --- | --- | --- | ---: | ---: | ---: | ---: | ---: |
{% for row in pathological_examples %}
| `{{ row.get("binary_id", "") }}` | `{{ row.get("address", "") }}` | `{{ row.get("name", "") }}` | `{{ row.get("giant_function_speed_family", "") }}` | {{ "%.3f"|format(row.get("build_duration_ms", 0.0) | float) }} | {{ "%.3f"|format(row.get("normalize_duration_ms", 0.0) | float) }} | {{ "%.3f"|format(row.get("structuring_duration_ms", 0.0) | float) }} | {{ "%.3f"|format(row.get("render_duration_ms", 0.0) | float) }} | {{ row.get("rendered_code_len", 0) }} |
{% endfor %}
{% endif %}

## Watchlist Selection Reasons

| Reason | Count |
| --- | ---: |
{% for key, value in (payload.get("watchlist_reason_counts", {}) or {})|dictsort %}
| `{{ key }}` | {{ value }} |
{% endfor %}

## Per-Binary Summary

| ID | Arch | Role | Avg norm sim | Coverage | Row gate | Watchlist | Eligibility |
| --- | --- | --- | ---: | ---: | --- | --- | --- |
{% for item in binaries %}
| `{{ item.get("id", "") }}` | `{{ item.get("arch", "unknown") }}` | `{{ item.get("role", "") }}` | {{ "%.3f"|format(item.get("avg_normalized_similarity", 0.0) | float) }}% | {{ "%.3f"|format(item.get("coverage_ratio_pct", 0.0) | float) }}% | `{{ item.get("row_fidelity_gate_status", "unknown") }}` | `{{ item.get("watchlist_source", "unknown") }}` | `{{ (item.get("eligibility", {}) or {}).get("reason", "unknown") }}` |
{% endfor %}

## Artifacts

- `benchmark_summary.json`
- `benchmark_summary.md`
- `benchmark_compact_summary.json`
