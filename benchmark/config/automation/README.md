# Automation config (manifest pointers)

Canonical lane manifest for `fission-automation nir-check` lives in the crate:

`crates/fission-automation/config/sentinel_sets.toml`

Paths inside that file remain **repository-root-relative** (for example binaries under `benchmark/binary/…`).
