/* WARNING: Unknown calling convention */

void print_3d_matrix(int depth,int rows,int cols)

{
  int in_ECX;
  int in_EDX;
  int in_R8D;
  int c;
  int r;
  int d;
  
  for (d = 0; d < in_ECX; d = d + 1) {
    printf("Layer %d:\n",(ulonglong)(uint)d);
    for (r = 0; r < in_EDX; r = r + 1) {
      for (c = 0; c < in_R8D; c = c + 1) {
        if ((c != 0) || (r < 1)) {
          printf("%d ",(ulonglong)(uint)(d * in_EDX * in_R8D + r * in_R8D + c));
        }
      }
      putchar(10);
    }
  }
  return;
}